<!-- <% layout("/layouts/boilerPlate") %>
<body>
  <div class="row mt-1.5">
   
    <div class="card col-6 offset-3">
      <img
        src="<%= listing.image.url %>"
        class="card-img-top"
        style="height: 20rem"
        alt="Listing-image"
      />
      <div class="card-body">
        <p class="card-text">
          <b><%= listing.title %></b>
          <br />
          &#8377;<%= listing.price.toLocaleString("en-IN") %>
          <br />
          <%= listing.location %>
          <br />
          <%= listing.country %>
        </p>
      </div>
    </div>
    <div class="btns">
      <form action="/listing/<%= listing._id%>/edit">
        <button class="btn btn-dark">Edit Listing</button>
      </form>
      <form method="post" action="/listing/<%= listing._id %>?_method=DELETE">
        <button class="btn btn-dark delete-listing-btn">Delete Listing</button>
      </form>
    </div>
</body> -->


<!-- <% layout("/layouts/boilerPlate") %>
<body>
  <div class="container mt-1.5">
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <div class="card">
          <img src="<%= listing.image.url %>" class="card-img-top" style="height: 20rem" alt="Listing-image">
          <div class="card-body">
            <h5 class="card-title"><%= listing.title %></h5>
            <p class="card-text">
              &#8377;<%= listing.price.toLocaleString("en-IN") %><br>
              <%= listing.location %><br>
              <%= listing.country %>
            </p>
          </div>
          <div class="card-footer d-flex justify-content-between align-items-center">
            <form action="/listing/<%= listing._id%>/edit">
              <button class="btn btn-primary">Edit Listing</button>
            </form>
            <form method="post" action="/listing/<%= listing._id %>?_method=DELETE">
              <button class="btn btn-danger">Delete Listing</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</body> -->

<% layout("/layouts/boilerPlate") %>
<body>
  <div class="container mt-1.5">
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <div class="card">
          <img src="<%= listing.image.url %>" class="card-img-top" style="height: 20rem" alt="Listing-image">
          <div class="card-body">
            <h5 class="card-title"><%= listing.title %></h5>
            <p class="card-text">
              &#8377;<%= listing.price.toLocaleString("en-IN") %><br>
              <%= listing.location %><br>
              <%= listing.country %>
            </p>
          </div>

          <div class="d-flex mt-3">
            <form action="/listing/<%= listing._id%>/edit">
              <button class="btn add-btn me-4 mt-0">Edit Listing</button>
            </form>
            <form method="post" action="/listing/<%= listing._id %>?_method=DELETE">
              <button class="btn btn-dark mt-0">Delete Listing</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>



